#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: integers to multiply
 * @b: integer to multiply
 * Return: The value of the multiplication between a and b
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
